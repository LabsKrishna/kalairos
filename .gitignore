dashboard.html
TEMP_CHANGELOG.md
node_modules/
.DS_Store
*.log
data.kalairos
data.kalairos.tmp
data.dbx
data.dbx.tmp
CLAUDE.md
STATE.md

# Standalone apps developed in-place — separate deployables with their own
# repos/histories, not library examples. Kept out of the npm package repo.
# personal-agent (vanilla, DEPRECATED 2026-07-14): lives deployed in the
#   kalairos.com repo's agent/personal-agent branch; all new work happens in
#   LabsKrishna/kalairos-personal-agent (Next.js).
examples/personal-agent/
examples/agent-platform/
# The agent platform (private LabsKrishna/kalairos_agent_platform repo, its own
# .git) mounted in-place: kernel/ (Python) + api/ (Node) + web/ (Next.js app).
services/

# Internal planning docs — development-only, not shipped
docs/storage-architecture-jsonl-sqlite-hybrid.md
docs/sqlite-hybrid-index-tickets.md
docs/phase-4.4-durable-runtime-spec.md

# Audit / positioning notes — strategy content, local-only like CLAUDE.md and STATE.md
docs/audit/

# Python (agent-platform epic — python/)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
# Observability benchmark output — regenerated by python/bench/observability.py;
# carries per-run ids so it churns on every run. The floors live in BENCH.md.
python/bench/observability-results.json

# CodeGraph index — machine-generated, rebuilt from source on demand
.codegraph/

# Local agent tooling — launch config and project skills, per-machine
.claude/

# Design source — a 12 MB binary, not part of the engine or the package
Design.pdf

# Discovery notes — call scripts and transcripts, local-only
docs/discovery/

# Next.js build output — this repo is the engine, but the app under
# services/web/ traces to a root that can land a .next/ here.
.next/
